import { Box } from "@mui/material"

export const Title = () => {
    return (
        <Box 
        component="h1"
        sx={{
            color: "black"
        }}>
            Sign in
        </Box>
    )
}